{
  "report_prefix": {
    "description": "告状消息的前缀文本",
    "type": "string",
    "default": "【🤖 机器人告状】"
  },
  "fallback_admin_umo": {
    "description": "备用管理员完整UMO",
    "type": "string",
    "default": "",
    "hint": "（当所有常规管理员发送失败时使用）格式示例：aiocqhttp:private:123456789"
  },
  "fallback_send_mode": {
    "description": "备用管理员发送内容模式",
    "type": "string",
    "options": ["only_error", "only_complaint", "both"],
    "default": "only_error",
    "hint": "only_error是只发送错误信息，only_complaint是只发送告状信息，both是上述两个都发"
  }
}
